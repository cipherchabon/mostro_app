import '../../../widgets/widgets.dart';

class EnterPinScreen extends ConsumerWidget {
  const EnterPinScreen({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Scaffold(
      body: const Center(
        child: Text('EnterPinScreen'),
      ),
    );
  }
}
